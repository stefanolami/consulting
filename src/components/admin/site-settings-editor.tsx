'use client'

import { LoaderCircle, Plus, Trash2 } from 'lucide-react'
import { useActionState, useState } from 'react'

import { saveSiteSettingsAction } from '@/app/(admin)/admin/settings/actions'
import type { SiteSettingsActionState } from '@/app/(admin)/admin/settings/actions'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'

const locales = ['en', 'de', 'it', 'pt-BR', 'pt-PT'] as const
type Social = { platform: 'linkedin' | 'instagram' | 'facebook' | 'youtube' | 'x'; url: string }
type Cta = { key: string; href: string; labels: { en: string; de: string | null; it: string | null; 'pt-BR': string | null; 'pt-PT': string | null } }
type Props = { contact: { email: string | null; phone: string | null; address: string | null; footerNote: string | null }; poeUrl: string; socials: Social[]; callsToAction: Cta[] }
const initial: SiteSettingsActionState = {}

export function SiteSettingsEditor({ contact, poeUrl, socials: initialSocials, callsToAction: initialCtas }: Props) {
	const [socials, setSocials] = useState(initialSocials); const [ctas, setCtas] = useState(initialCtas)
	const [state, action, pending] = useActionState(saveSiteSettingsAction, initial)
	return <form action={action} className="mt-9 space-y-7"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 className="font-robo text-2xl">Contact and footer details</h2><p className="mt-1 text-sm text-slate-600">Global content only. Footer layout, legal links and styling remain in code.</p><div className="mt-6 grid gap-5 md:grid-cols-2"><Field label="Contact email"><Input defaultValue={contact.email ?? ''} name="email" type="email" /></Field><Field label="Contact phone"><Input defaultValue={contact.phone ?? ''} name="phone" /></Field><Field className="md:col-span-2" label="Postal address"><textarea className="input-textarea" defaultValue={contact.address ?? ''} name="address" rows={4} /></Field><Field className="md:col-span-2" label="Footer note"><Input defaultValue={contact.footerNote ?? ''} maxLength={500} name="footerNote" /></Field></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 className="font-robo text-2xl">POE external link</h2><p className="mt-1 text-sm text-slate-600">The global destination used by code-owned POE links.</p><div className="mt-5"><Field label="Complete web URL"><Input defaultValue={poeUrl} name="poeUrl" placeholder="https://…" required type="url" /></Field></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-robo text-2xl">Social links</h2><p className="mt-1 text-sm text-slate-600">One approved URL per supported platform.</p></div><Button onClick={() => setSocials((current) => [...current, { platform: 'linkedin', url: '' }])} type="button" variant="outline"><Plus />Add social link</Button></div><input name="socialLinks" type="hidden" value={JSON.stringify(socials)} /><div className="mt-5 space-y-3">{socials.map((social, index) => <div className="grid gap-3 rounded-lg bg-slate-50 p-4 md:grid-cols-[12rem_1fr_auto] md:items-end" key={index}><Field label="Platform"><select className="input-select" onChange={(event) => setSocials((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, platform: event.target.value as Social['platform'] } : item))} value={social.platform}>{['linkedin', 'instagram', 'facebook', 'youtube', 'x'].map((platform) => <option key={platform} value={platform}>{platform}</option>)}</select></Field><Field label="Profile URL"><Input onChange={(event) => setSocials((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, url: event.target.value } : item))} type="url" value={social.url} /></Field><Button aria-label="Remove social link" onClick={() => setSocials((current) => current.filter((_, itemIndex) => itemIndex !== index))} size="icon" type="button" variant="ghost"><Trash2 /></Button></div>)}{!socials.length && <p className="text-sm text-slate-500">No social links are configured.</p>}</div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-robo text-2xl">Approved calls to action</h2><p className="mt-1 text-sm text-slate-600">Reusable keys, destinations and localized labels. Placement and presentation remain in code.</p></div><Button onClick={() => setCtas((current) => [...current, { key: '', href: '', labels: { en: '', de: null, it: null, 'pt-BR': null, 'pt-PT': null } }])} type="button" variant="outline"><Plus />Add call to action</Button></div><input name="callsToAction" type="hidden" value={JSON.stringify(ctas)} /><div className="mt-5 space-y-4">{ctas.map((cta, index) => <div className="rounded-lg border border-slate-200 p-4" key={index}><div className="grid gap-3 md:grid-cols-2"><Field label="Stable key"><Input onChange={(event) => setCtas((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, key: event.target.value } : item))} placeholder="e.g. contact-us" value={cta.key} /></Field><Field label="Destination"><Input onChange={(event) => setCtas((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, href: event.target.value } : item))} placeholder="/contact or https://…" value={cta.href} /></Field>{locales.map((locale) => <Field key={locale} label={`${locale} label${locale === 'en' ? '' : ' (optional)'}`}><Input onChange={(event) => setCtas((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, labels: { ...item.labels, [locale]: event.target.value || null } } : item))} value={cta.labels[locale] ?? ''} /></Field>)}</div><Button className="mt-3" onClick={() => setCtas((current) => current.filter((_, itemIndex) => itemIndex !== index))} type="button" variant="ghost"><Trash2 />Remove call to action</Button></div>)}{!ctas.length && <p className="text-sm text-slate-500">No reusable calls to action are configured.</p>}</div></section>{state.error && <p className="rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{state.error}</p>}{state.success && <p className="rounded-md border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{state.success}</p>}<Button disabled={pending} type="submit">{pending && <LoaderCircle className="animate-spin" />}Save global settings</Button></form>
}

function Field({ label, children, className }: { label: string; children: React.ReactNode; className?: string }) { return <label className={`grid gap-1.5 text-sm font-medium text-slate-700 ${className ?? ''}`}><span>{label}</span>{children}</label> }
