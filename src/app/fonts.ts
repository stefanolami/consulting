// Keep the layout API stable without requiring a build-time Google Fonts request.
// The corresponding system-font stacks are defined in globals.css.
export const jose = { variable: '' }
export const unna = { variable: '' }
export const robo = { variable: '' }
